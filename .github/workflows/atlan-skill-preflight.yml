name: Atlan Agent Registry preflight

on:
  pull_request_target:
    types: [opened, synchronize, reopened]
    paths:
      - "skills/**"
      - ".atlan/package.json"

permissions: {}

concurrency:
  group: atlan-agent-registry-preflight-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  comment:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Register Atlan Agent Registry preflight
        uses: atlanai/agent-registry-action@a93207c5d446911ab31504bad8c000222898755d # fix/installer-digest-v0.1.2
        with:
          mode: preflight
          github-token: ${{ github.token }}
